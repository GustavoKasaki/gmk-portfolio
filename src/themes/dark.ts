const dark = {
  mainColor: '#fff',
  secColor: '#eee',
  bgColor: '#282a35',
  btnBgColor: '#fff',
  borderColor: '#c1c1c1'
}

export default dark

export type Theme = {
  mainColor: string
  secColor: string
  bgColor: string
  btnBgColor: string
  borderColor: string
}
